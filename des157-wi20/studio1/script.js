(function() {
    'use strict';
    console.log("hullo this is a test");
}());

/**document.getElementById("plant-form").addEventListener("submit", function(event)    {
    // Prevent the default behavior from occuring:
    event.preventDefault();

    // Find style choice:
    var styleList = document.querySelectorAll('input[name="type"]');
    var userStyle;
    for (let i = 0; i < styleList.length; i++)  {
        if (styleList[i].checked)   {
            userStyle = styleList[i].value;
            break;
        }
    }
});**/